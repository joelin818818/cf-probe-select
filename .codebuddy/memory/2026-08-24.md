# 2026-08-24

## cf-probe-select 项目（GitHub: joelin818818/cf-probe-select）

- 项目定位：CF 探测优选 —— 自动挖掘走 Cloudflare 的第三方站点，后续规划测速+推优排序。
- 命名确认：`cf-probe-select`（CF 探测优选），仓库已建，本地 git 已 init 并推到 origin/main。
- 本地代理端口 10808（SOCKS5），已写入仓库 `--local` git 配置：`http.proxy=socks5://127.0.0.1:10808`、`https.proxy=socks5://127.0.0.1:10808`。直连网络失败时改用代理可 push。
- GitHub Actions 已配置 `.github/workflows/probe.yml`：每天 UTC 00:00 + 手动触发，`timeout 120` 限 2 分钟，`permissions: contents: write`，跑完 commit 回推 `cf_domains.txt`。

## 关键需求决策（用户确认）
- txt 输出：**仅纯域名，无描述、无序号**（用户放弃中文字典/AI 描述方案，改英文思路后又决定不要描述）。
- 种子策略：**自举滚动**——txt 非空则随机抽样历史域名作种子；为空则用内置 `BOOTSTRAP_SEEDS=["cloudflare.com"]`。**不写死固定种子列表、不读"最后一行"续跑**。
- 主域名聚合：**同一 registered domain 最多保留 3 个子域名**（保留此限制）。
- 敏感信息：仓库公开，**严禁硬编码密钥**；如需密钥走 GitHub Actions Secrets，README 已写明设置教程（Settings → Secrets and variables → Actions）。
- 初始 `cf_domains.txt` 仅含注释，首次 Actions 从 cloudflare.com 自举。

## 文件结构（根目录）
- `cf_probe_select.py`（阶段一，已重写：纯域名/自举/主域≤3/边探边写）
- `probe_speed.py`、`select_rank.py`（阶段二、三占位，未实现）
- `cf_domains.txt`（纳入 git，自动累积）、`README.md`、`.gitignore`（已放开 cf_domains.txt）、`LICENSE`

## 首次 Actions 运行反馈与优化（已推 c097511）
- 首次跑 timeout 124（正常 2 分钟到时），但被 cloudflare.com 自举出的巨型站（github/linkedin/youtube 等）外链淹没，命中 CF 第三方偏少。
- 优化（用户确认全部采纳 + 加巨型站黑名单）：
  1. 非 CF 域名检测完即丢弃，**不抓页面扩散**；只 CF 官方域名 + 命中 CF 第三方域名才继续抓页扩散。
  2. 单页嗅探入队上限 `MAX_NEW_PER_PAGE = 50`。
  3. 双队列（cf_q 优先），命中 CF 的域名优先扩散。
  4. 巨型科技站黑名单 `BIG_TECH_ROOTS`（google/meta/msft/github/apple/amazon 等走 CF 但非冷门目标），直接跳过不入库不扩散。
  5. suppress `XMLParsedAsHTMLWarning`。
- 目标定位明确为"CF 冷门优选"，过滤知名巨型站。
